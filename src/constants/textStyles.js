import { StyleSheet } from 'react-native';

const textStyles = StyleSheet.create({
  smallText: {
    fontSize: 16,
  },
  largeText: {
    fontSize: 20,
  },
  heading: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 10,
  },
  subheading: {
    lineHeight: 22,
    marginBottom: 20,
  },
});

export default textStyles;
